namespace Shrooms.Contracts.Enums
{
    /// <summary>
    /// The file a leave order is handed over as. Word is the one payroll files;
    /// the PDF is the same page, laid out, for reading and printing.
    /// </summary>
    public enum VacationDocumentFormat
    {
        Word = 0,
        Pdf = 1
    }
}
